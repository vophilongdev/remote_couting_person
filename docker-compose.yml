version: '3.8'

services:
  people-counting:
    image: harbor.tado.vn/long_dev/people_counting:latest
    container_name: people_counting
    network_mode: "host"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - API_URL=https://api-dev.tado.vn/api/camera-statistics
      - SESSION_TOKEN=359c8b594fe339f1d7b8337ad22f4ac1
      - MODEL_PATH=ai_core/weights/yolo_best2.pt
      - CONF=0.8
      - LD_LIBRARY_PATH=/usr/local/lib/python3.10/site-packages/NetSDK/Libs/linux64:/app/ai_core/services/sdk/dist
    volumes:
      - ./libplay.so:/usr/local/lib/python3.10/site-packages/NetSDK/Libs/linux64/libplay.so
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/" ]
      interval: 10s
      timeout: 5s
      retries: 3
